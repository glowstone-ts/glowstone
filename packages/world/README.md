# @dripleaf/world

Replacement for **prismarine-world**: chunk sections, palettes, `World.getBlock`, `World.getBiome`, and chunk data parsing from protocol packets.

```ts
import { World, parseChunkSections, applyLevelChunk } from "@dripleaf/world"
```
