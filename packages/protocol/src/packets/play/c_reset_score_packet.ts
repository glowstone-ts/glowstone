// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundResetScorePacket extends DripleafPacket {
	static readonly id = 0x4f;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundResetScorePacket.id;
	override readonly state = ClientboundResetScorePacket.state;
	override readonly direction = ClientboundResetScorePacket.direction;

	constructor(
		public owner: string,
		public objectiveName: string | null
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.owner);
		writer.writePrefixedOptional(this.objectiveName, (objectiveName) => writer.writeString(objectiveName));
	}

	static read(reader: PacketReader): ClientboundResetScorePacket {
		const owner = reader.readString();
		const objectiveName = reader.readPrefixedOptional(() => reader.readString());
		return new ClientboundResetScorePacket(owner, objectiveName);
	}
}