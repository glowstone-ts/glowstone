// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

export class ClientboundMoveEntityPosPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundMoveEntityPosPacket>({
		encode(writer: PacketWriter, value: ClientboundMoveEntityPosPacket) {
		writer.writeVarInt(value.entityId);
		writer.writeShort(value.deltaX);
		writer.writeShort(value.deltaY);
		writer.writeShort(value.deltaZ);
		writer.writeBoolean(value.onGround);
		},
		decode(reader: PacketReader): ClientboundMoveEntityPosPacket {
		const entityId = reader.readVarInt();
		const deltaX = reader.readShort();
		const deltaY = reader.readShort();
		const deltaZ = reader.readShort();
		const onGround = reader.readBoolean();
		return new ClientboundMoveEntityPosPacket(entityId, deltaX, deltaY, deltaZ, onGround);
		},
	});

	constructor(
		public entityId: number,
		public deltaX: number,
		public deltaY: number,
		public deltaZ: number,
		public onGround: boolean
	) {
		super();
	}

}
