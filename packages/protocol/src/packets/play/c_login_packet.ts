// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import { CommonPlayerSpawnInfo } from '../../datatypes/CommonPlayerSpawnInfo';

export class ClientboundLoginPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundLoginPacket>({
		encode(writer: PacketWriter, value: ClientboundLoginPacket) {
		writer.writeInt(value.entityId);
		writer.writeBoolean(value.isHardcore);
		writer.writeArray(value.dimensions, dimension => writer.writeIdentifier(dimension));
		writer.writeVarInt(value.maxPlayers);
		writer.writeVarInt(value.viewDistance);
		writer.writeVarInt(value.simulationDistance);
		writer.writeBoolean(value.reducedDebugInfo);
		writer.writeBoolean(value.enableRespawnScreen);
		writer.writeBoolean(value.doLimitedCrafting);
		writer.writeCodec(CommonPlayerSpawnInfo.codec, value.commonPlayerSpawnInfo);
		writer.writeVarInt(value.portalCooldown);
		writer.writeVarInt(value.seaLevel);
		},
		decode(reader: PacketReader): ClientboundLoginPacket {
		const entityId = reader.readInt();
		const isHardcore = reader.readBoolean();
		const dimensions = reader.readArray(() => reader.readIdentifier());
		const maxPlayers = reader.readVarInt();
		const viewDistance = reader.readVarInt();
		const simulationDistance = reader.readVarInt();
		const reducedDebugInfo = reader.readBoolean();
		const enableRespawnScreen = reader.readBoolean();
		const doLimitedCrafting = reader.readBoolean();
		const commonPlayerSpawnInfo = reader.readCodec(CommonPlayerSpawnInfo.codec);
		const portalCooldown = reader.readVarInt();
		const seaLevel = reader.readVarInt();
		return new ClientboundLoginPacket(entityId, isHardcore, dimensions, maxPlayers, viewDistance, simulationDistance, reducedDebugInfo, enableRespawnScreen, doLimitedCrafting, commonPlayerSpawnInfo, portalCooldown, seaLevel);
		},
	});

	constructor(
		public entityId: number,
		public isHardcore: boolean,
		public dimensions: import('@dripleaf/registry').Identifier[],
		public maxPlayers: number,
		public viewDistance: number,
		public simulationDistance: number,
		public reducedDebugInfo: boolean,
		public enableRespawnScreen: boolean,
		public doLimitedCrafting: boolean,
		public commonPlayerSpawnInfo: CommonPlayerSpawnInfo,
		public portalCooldown: number,
		public seaLevel: number
	) {
		super();
	}

}
