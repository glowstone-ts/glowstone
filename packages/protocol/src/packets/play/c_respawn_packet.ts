// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';
import { CommonPlayerSpawnInfo } from '../../datatypes/CommonPlayerSpawnInfo';

enum DataToKeep {
	KeepAttributeModifiers = 0x01,
	KeepInventory = 0x02,
	KeepEnderChestContents = 0x03,
}

export class ClientboundRespawnPacket extends DripleafPacket {
	static readonly id = 0x52;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundRespawnPacket.id;
	override readonly state = ClientboundRespawnPacket.state;
	override readonly direction = ClientboundRespawnPacket.direction;

	constructor(
		public commonPlayerSpawnInfo: CommonPlayerSpawnInfo,
		public dataToKeep: DataToKeep
	) {
		super();
	}

	write(writer: PacketWriter) {
		this.commonPlayerSpawnInfo.write(writer);
		writer.writeByte(this.dataToKeep);
	}

	static read(reader: PacketReader): ClientboundRespawnPacket {
		const commonPlayerSpawnInfo = CommonPlayerSpawnInfo.read(reader);
		const dataToKeep = reader.readByte() as DataToKeep;
		return new ClientboundRespawnPacket(commonPlayerSpawnInfo, dataToKeep);
	}
}