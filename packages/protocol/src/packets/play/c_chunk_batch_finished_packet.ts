// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundChunkBatchFinishedPacket extends DripleafPacket {
	static readonly id = 0x0b;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundChunkBatchFinishedPacket.id;
	override readonly state = ClientboundChunkBatchFinishedPacket.state;
	override readonly direction = ClientboundChunkBatchFinishedPacket.direction;

	constructor(
		public batchSize: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.batchSize);
	}

	static read(reader: PacketReader): ClientboundChunkBatchFinishedPacket {
		const batchSize = reader.readVarInt();
		return new ClientboundChunkBatchFinishedPacket(batchSize);
	}
}