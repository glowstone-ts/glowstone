// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundContainerSetDataPacket extends DripleafPacket {
	static readonly id = 0x13;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundContainerSetDataPacket.id;
	override readonly state = ClientboundContainerSetDataPacket.state;
	override readonly direction = ClientboundContainerSetDataPacket.direction;

	constructor(
		public windowId: number,
		public property: number,
		public value: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.windowId);
		writer.writeShort(this.property);
		writer.writeShort(this.value);
	}

	static read(reader: PacketReader): ClientboundContainerSetDataPacket {
		const windowId = reader.readVarInt();
		const property = reader.readShort();
		const value = reader.readShort();
		return new ClientboundContainerSetDataPacket(windowId, property, value);
	}
}