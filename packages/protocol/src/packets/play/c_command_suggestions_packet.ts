// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import type { UnnamedNbtTag } from '@dripleaf/nbt';

type MatchEntry = {
	match: string;
	tooltip: UnnamedNbtTag | null;
}

export class ClientboundCommandSuggestionsPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundCommandSuggestionsPacket>({
		encode(writer: PacketWriter, value: ClientboundCommandSuggestionsPacket) {
		writer.writeVarInt(value.transactionId);
		writer.writeVarInt(value.start);
		writer.writeVarInt(value.length);
		writer.writeArray(value.matches, (entry) => {
			writer.writeString(entry.match);
			writer.writePrefixedOptional(entry.tooltip, (tooltip) => writer.writeNbt(tooltip));
		});
		},
		decode(reader: PacketReader): ClientboundCommandSuggestionsPacket {
		const transactionId = reader.readVarInt();
		const start = reader.readVarInt();
		const length = reader.readVarInt();
		const matches = reader.readArray(() => {
			const match = reader.readString();
			const tooltip = reader.readPrefixedOptional(() => reader.readNbt());
			return { match, tooltip };
		});
		return new ClientboundCommandSuggestionsPacket(transactionId, start, length, matches);
		},
	});

	constructor(
		public transactionId: number,
		public start: number,
		public length: number,
		public matches: MatchEntry[]
	) {
		super();
	}

}
