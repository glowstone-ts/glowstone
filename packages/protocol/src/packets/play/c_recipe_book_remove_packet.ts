// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundRecipeBookRemovePacket extends DripleafPacket {
	static readonly id = 0x4b;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundRecipeBookRemovePacket.id;
	override readonly state = ClientboundRecipeBookRemovePacket.state;
	override readonly direction = ClientboundRecipeBookRemovePacket.direction;

	constructor(
		public recipes: number[]
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeArray(this.recipes, (recipe) => writer.writeVarInt(recipe));
	}

	static read(reader: PacketReader): ClientboundRecipeBookRemovePacket {
		const recipes = reader.readArray(() => reader.readVarInt());
		return new ClientboundRecipeBookRemovePacket(recipes);
	}
}