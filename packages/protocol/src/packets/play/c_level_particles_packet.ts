// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

export class ClientboundLevelParticlesPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundLevelParticlesPacket>({
		encode(writer: PacketWriter, value: ClientboundLevelParticlesPacket) {
		writer.writeBoolean(value.longDistance);
		writer.writeBoolean(value.alwaysShow);
		writer.writeDouble(value.x);
		writer.writeDouble(value.y);
		writer.writeDouble(value.z);
		writer.writeFloat(value.offsetX);
		writer.writeFloat(value.offsetY);
		writer.writeFloat(value.offsetZ);
		writer.writeFloat(value.maxSpeed);
		writer.writeInt(value.count);
		writer.writeVarInt(value.particleId);
		},
		decode(reader: PacketReader): ClientboundLevelParticlesPacket {
		const longDistance = reader.readBoolean();
		const alwaysShow = reader.readBoolean();
		const x = reader.readDouble();
		const y = reader.readDouble();
		const z = reader.readDouble();
		const offsetX = reader.readFloat();
		const offsetY = reader.readFloat();
		const offsetZ = reader.readFloat();
		const maxSpeed = reader.readFloat();
		const count = reader.readInt();
		const particleId = reader.readVarInt();
		return new ClientboundLevelParticlesPacket(longDistance, alwaysShow, x, y, z, offsetX, offsetY, offsetZ, maxSpeed, count, particleId);
		},
	});

	constructor(
		public longDistance: boolean,
		public alwaysShow: boolean,
		public x: number,
		public y: number,
		public z: number,
		public offsetX: number,
		public offsetY: number,
		public offsetZ: number,
		public maxSpeed: number,
		public count: number,
		public particleId: number, // todo: link with registry key instead of numeric id
	) {
		super();
	}

}
