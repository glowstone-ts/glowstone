// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { type PacketReader, type PacketWriter, Codecs } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import { ItemStackCodec } from '../common';
import type { ItemStack } from '@dripleaf/inventory';

type ChangedSlot = {
	slotIndex: number;
	item: ItemStack;
};

const changedSlotCodec = Codecs.map<number, ItemStack>(Codecs.short, ItemStackCodec);

export class ServerboundContainerClickPacket extends DripleafPacket {
	static readonly codec = packetCodec({
		encode(writer: PacketWriter, value: ServerboundContainerClickPacket) {
			writer.writeVarInt(value.containerId);
			writer.writeVarInt(value.stateId);
			writer.writeShort(value.slotNum);
			writer.writeByte(value.buttonNum);
			writer.writeVarInt(value.containerInput);
			changedSlotCodec.encode(writer, value.changedSlots);
			ItemStackCodec.encode(writer, value.carriedItem);
		},
		decode(reader: PacketReader): ServerboundContainerClickPacket {
			const containerId = reader.readVarInt();
			const stateId = reader.readVarInt();
			const slotNum = reader.readShort();
			const buttonNum = reader.readByte();
			const containerInput = reader.readVarInt();
			const changedSlots = changedSlotCodec.decode(reader);
			const carriedItem = ItemStackCodec.decode(reader);
			return new ServerboundContainerClickPacket(containerId, stateId, slotNum, buttonNum, containerInput, changedSlots, carriedItem);
		},
	});

	constructor(
		public containerId: number,
		public stateId: number,
		public slotNum: number,
		public buttonNum: number,
		public containerInput: number,
		public changedSlots: Map<number, ItemStack>,
		public carriedItem: ItemStack,
	) {
		super();
	}
}