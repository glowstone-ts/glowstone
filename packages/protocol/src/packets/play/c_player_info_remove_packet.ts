// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundPlayerInfoRemovePacket extends DripleafPacket {
	static readonly id = 0x45;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundPlayerInfoRemovePacket.id;
	override readonly state = ClientboundPlayerInfoRemovePacket.state;
	override readonly direction = ClientboundPlayerInfoRemovePacket.direction;

	constructor(
		public profileIds: string[]
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeArray(this.profileIds, (id) => writer.writeUUID(id));
	}

	static read(reader: PacketReader): ClientboundPlayerInfoRemovePacket {
		const profileIds = reader.readArray(() => reader.readUUID());
		return new ClientboundPlayerInfoRemovePacket(profileIds);
	}
}