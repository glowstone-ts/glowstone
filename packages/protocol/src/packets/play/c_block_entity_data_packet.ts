// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import type { UnnamedNbtTag } from '@dripleaf/nbt';
import type { Vec3 } from 'vec3';

export class ClientboundBlockEntityDataPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundBlockEntityDataPacket>({
		encode(writer: PacketWriter, value: ClientboundBlockEntityDataPacket) {
		writer.writeBlockPos(value.position);
		writer.writeVarInt(value.type);
		writer.writeNbt(value.data);
		},
		decode(reader: PacketReader): ClientboundBlockEntityDataPacket {
		const position = reader.readBlockPos();
		const type = reader.readVarInt();
		const data = reader.readNbt();
		return new ClientboundBlockEntityDataPacket(position, type, data);
		},
	});

	constructor(
		public position: Vec3,
		public type: number, // todo: registry
		public data: UnnamedNbtTag
	) {
		super();
	}

}
