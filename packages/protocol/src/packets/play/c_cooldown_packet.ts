// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundCooldownPacket extends DripleafPacket {
	static readonly id = 0x16;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundCooldownPacket.id;
	override readonly state = ClientboundCooldownPacket.state;
	override readonly direction = ClientboundCooldownPacket.direction;

	constructor(
		public cooldownGroup: string,
		public cooldownTicks: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.cooldownGroup);
		writer.writeVarInt(this.cooldownTicks);
	}

	static read(reader: PacketReader): ClientboundCooldownPacket {
		const cooldownGroup = reader.readString();
		const cooldownTicks = reader.readVarInt();
		return new ClientboundCooldownPacket(cooldownGroup, cooldownTicks);
	}
}