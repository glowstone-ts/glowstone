// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundSetBorderCenterPacket extends DripleafPacket {
	static readonly id = 0x58;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundSetBorderCenterPacket.id;
	override readonly state = ClientboundSetBorderCenterPacket.state;
	override readonly direction = ClientboundSetBorderCenterPacket.direction;

	constructor(
		public newCenterX: number,
		public newCenterZ: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeDouble(this.newCenterX);
		writer.writeDouble(this.newCenterZ);
	}

	static read(reader: PacketReader): ClientboundSetBorderCenterPacket {
		const newCenterX = reader.readDouble();
		const newCenterZ = reader.readDouble();
		return new ClientboundSetBorderCenterPacket(newCenterX, newCenterZ);
	}
}