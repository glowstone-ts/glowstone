// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { type PacketReader, type PacketWriter, Codecs } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import type { Identifier } from '@dripleaf/registry';

export class ClientboundCustomPayloadPacket extends DripleafPacket {
	static readonly codec = packetCodec({
		encode(writer: PacketWriter, value: ClientboundCustomPayloadPacket) {
			Codecs.identifier.encode(writer, value.channel);
			writer.writeBytes(value.data);
		},
		decode(reader: PacketReader): ClientboundCustomPayloadPacket {
			return new ClientboundCustomPayloadPacket(
				Codecs.identifier.decode(reader),
				reader.readRemaining(),
			);
		},
	});

	constructor(
		public channel: Identifier,
		public data: Uint8Array
	) {
		super();
	}
}
