// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundCustomPayloadPacket extends DripleafPacket {
	static readonly id = 0x18;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundCustomPayloadPacket.id;
	override readonly state = ClientboundCustomPayloadPacket.state;
	override readonly direction = ClientboundCustomPayloadPacket.direction;

	constructor(
		public channel: string,
		public data: Uint8Array
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.channel);
		writer.writeByteArray(this.data);
	}

	static read(reader: PacketReader): ClientboundCustomPayloadPacket {
		const channel = reader.readString();
		const data = reader.readByteArray();
		return new ClientboundCustomPayloadPacket(channel, data);
	}
}