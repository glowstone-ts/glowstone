// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

type StatisticEntry = {
	category: number;
	id: number;
	value: number;
};

export class ClientboundAwardStatsPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundAwardStatsPacket>({
		encode(writer: PacketWriter, value: ClientboundAwardStatsPacket) {
		writer.writeArray(value.statistics, (stat) => {
			writer.writeVarInt(stat.category);
			writer.writeVarInt(stat.id);
			writer.writeVarInt(stat.value);
		});
		},
		decode(reader: PacketReader): ClientboundAwardStatsPacket {
		const statistics = reader.readArray(() => {
			const category = reader.readVarInt();
			const id = reader.readVarInt();
			const value = reader.readVarInt();
			return { category, id, value };
		});
		return new ClientboundAwardStatsPacket(statistics);
		},
	});

	constructor(
		public statistics: StatisticEntry[]
	) {
		super();
	}

}
