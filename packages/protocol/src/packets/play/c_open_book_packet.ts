// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

enum Hand {
	MainHand = 0,
	OffHand = 1
}

export class ClientboundOpenBookPacket extends DripleafPacket {
	static readonly id = 0x3a;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundOpenBookPacket.id;
	override readonly state = ClientboundOpenBookPacket.state;
	override readonly direction = ClientboundOpenBookPacket.direction;

	constructor(
		public hand: Hand
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.hand);
	}

	static read(reader: PacketReader): ClientboundOpenBookPacket {
		const hand = reader.readVarInt();
		return new ClientboundOpenBookPacket(hand);
	}
}