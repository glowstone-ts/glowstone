// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundContainerClosePacket extends DripleafPacket {
	static readonly id = 0x11;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundContainerClosePacket.id;
	override readonly state = ClientboundContainerClosePacket.state;
	override readonly direction = ClientboundContainerClosePacket.direction;

	constructor(
		public windowId: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.windowId);
	}

	static read(reader: PacketReader): ClientboundContainerClosePacket {
		const windowId = reader.readVarInt();
		return new ClientboundContainerClosePacket(windowId);
	}
}