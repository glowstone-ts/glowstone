// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

export class ClientboundPlayerRotationPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundPlayerRotationPacket>({
		encode(writer: PacketWriter, value: ClientboundPlayerRotationPacket) {
		writer.writeFloat(value.yaw);
		writer.writeBoolean(value.relativeYaw);
		writer.writeFloat(value.pitch);
		writer.writeBoolean(value.relativePitch);
		},
		decode(reader: PacketReader): ClientboundPlayerRotationPacket {
		const yaw = reader.readFloat();
		const relativeYaw = reader.readBoolean();
		const pitch = reader.readFloat();
		const relativePitch = reader.readBoolean();
		return new ClientboundPlayerRotationPacket(yaw, relativeYaw, pitch, relativePitch);
		},
	});

	constructor(
		public yaw: number,
		public relativeYaw: boolean,
		public pitch: number,
		public relativePitch: boolean
	) {
		super();
	}

}
