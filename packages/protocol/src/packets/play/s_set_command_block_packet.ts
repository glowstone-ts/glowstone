// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { BlockPos } from '@dripleaf/core';
import { BlockPosCodec } from '../../datatypes/BlockPos';
import { Codecs, type PacketReader, type PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

enum CommandBlockMode {
	SEQUENCE,
	AUTO,
	REDSTONE,
}

export class ServerboundSetCommandBlockPacket extends DripleafPacket {
	static readonly codec = packetCodec({
		encode(writer: PacketWriter, value: ServerboundSetCommandBlockPacket) {
			BlockPosCodec.encode(writer, value.pos);
			writer.writeString(value.command);
			Codecs.varIntEnum(CommandBlockMode).encode(writer, value.mode);
			let flags = 0;
			if (value.trackOutput) flags |= 1;
			if (value.conditional) flags |= 2;
			if (value.automatic) flags |= 4;
			writer.writeByte(flags);
		},
		decode(reader: PacketReader): ServerboundSetCommandBlockPacket {
			const pos = BlockPosCodec.decode(reader);
			const command = reader.readString();
			const mode = Codecs.varIntEnum(CommandBlockMode).decode(reader);
			const flags = reader.readByte();
			return new ServerboundSetCommandBlockPacket(
				pos,
				command,
				mode,
				(flags & 1) !== 0,
				(flags & 2) !== 0,
				(flags & 4) !== 0,
			);
		},
	});

	constructor(
		public pos: BlockPos,
		public command: string,
		public mode: number,
		public trackOutput: boolean,
		public conditional: boolean,
		public automatic: boolean,
	) {
		super();
	}

}
