// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundSetCameraPacket extends DripleafPacket {
	static readonly id = 0x5d;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundSetCameraPacket.id;
	override readonly state = ClientboundSetCameraPacket.state;
	override readonly direction = ClientboundSetCameraPacket.direction;

	constructor(
		public cameraId: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.cameraId);
	}

	static read(reader: PacketReader): ClientboundSetCameraPacket {
		const cameraId = reader.readVarInt();
		return new ClientboundSetCameraPacket(cameraId);
	}
}