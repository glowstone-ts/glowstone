// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundLightUpdatePacket extends DripleafPacket {
	static readonly id = 0x30;
	static readonly state = State.Play;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundLightUpdatePacket.id;
	override readonly state = ClientboundLightUpdatePacket.state;
	override readonly direction = ClientboundLightUpdatePacket.direction;

	constructor(
		public x: number,
		public z: number,
		public lightData: any // todo
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.x);
		writer.writeVarInt(this.z);
	}

	static read(reader: PacketReader): ClientboundLightUpdatePacket {
		const x = reader.readVarInt();
		const z = reader.readVarInt();
		return new ClientboundLightUpdatePacket(x, z, null);
	}
}