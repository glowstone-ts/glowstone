// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ServerboundCustomPayloadPacket extends DripleafPacket {
	static readonly id = 0x02;
	static readonly state = State.Configuration;
	static readonly direction = Direction.Serverbound;

	override readonly id = ServerboundCustomPayloadPacket.id;
	override readonly state = ServerboundCustomPayloadPacket.state;
	override readonly direction = ServerboundCustomPayloadPacket.direction;

	constructor(
		public channel: string,
		public data: Uint8Array,
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.channel);
		writer.writeByteArray(this.data);
	}

	static read(reader: PacketReader): ServerboundCustomPayloadPacket {
		const channel = reader.readString();
		const data = reader.readByteArray();
		return new ServerboundCustomPayloadPacket(channel, data);
	}
}