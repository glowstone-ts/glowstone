// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';
import type { PackEntry } from '../common';

export class ClientboundSelectKnownPacksPacket extends DripleafPacket {
	static readonly id = 0x0e;
	static readonly state = State.Configuration;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundSelectKnownPacksPacket.id;
	override readonly state = ClientboundSelectKnownPacksPacket.state;
	override readonly direction = ClientboundSelectKnownPacksPacket.direction;

	constructor(
		public knownPacks: PackEntry[],
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeArray(this.knownPacks, (pack) => {
			writer.writeString(pack.namespace);
			writer.writeString(pack.id);
			writer.writeVarInt(pack.version);
		});
	}

	static read(reader: PacketReader): ClientboundSelectKnownPacksPacket {
		const knownPacks = reader.readArray(() => {
			const namespace = reader.readString();
			const id = reader.readString();
			const version = reader.readVarInt();
			return { namespace, id, version };
		});
		return new ClientboundSelectKnownPacksPacket(knownPacks);
	}
}