// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';
import type { UUID } from 'node:crypto';

enum ResourcePackResult {
	SuccessfullyDownloaded = 0,
	Declined = 1,
	FailedToDownload = 2,
	Accepted = 3,
	Downloaded = 4,
	InvalidURL = 5,
	FailedToReload = 6,
	Discarded = 7
}

export class ServerboundResourcePackPacket extends DripleafPacket {
	static readonly id = 0x06;
	static readonly state = State.Configuration;
	static readonly direction = Direction.Serverbound;

	override readonly id = ServerboundResourcePackPacket.id;
	override readonly state = ServerboundResourcePackPacket.state;
	override readonly direction = ServerboundResourcePackPacket.direction;

	constructor(
		public uuid: UUID,
		public result: ResourcePackResult
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeUUID(this.uuid);
		writer.writeVarInt(this.result);
	}

	static read(reader: PacketReader): ServerboundResourcePackPacket {
		const uuid = reader.readUUID();
		const result = reader.readVarInt();
		return new ServerboundResourcePackPacket(uuid, result);
	}
}