// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

export class ServerboundKeepAlivePacket extends DripleafPacket {
	static readonly codec = packetCodec<ServerboundKeepAlivePacket>({
		encode(writer: PacketWriter, value: ServerboundKeepAlivePacket) {
		writer.writeLong(value.keepAliveId);
		},
		decode(reader: PacketReader): ServerboundKeepAlivePacket {
		const keepAliveId = reader.readLong();
		return new ServerboundKeepAlivePacket(keepAliveId);
		},
	});

	constructor(
		public keepAliveId: bigint
	) {
		super();
	}

}
