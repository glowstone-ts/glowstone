// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

type DetailEntry = {
	title: string;
	description: string;
}

export class ClientboundCustomReportDetailsPacket extends DripleafPacket {
	static readonly id = 0x0f;
	static readonly state = State.Configuration;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundCustomReportDetailsPacket.id;
	override readonly state = ClientboundCustomReportDetailsPacket.state;
	override readonly direction = ClientboundCustomReportDetailsPacket.direction;

	constructor(
		public details: DetailEntry[],
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeArray(this.details, (detail) => {
			writer.writeString(detail.title, 128);
			writer.writeString(detail.description, 4096);
		});
	}

	static read(reader: PacketReader): ClientboundCustomReportDetailsPacket {
		const details = reader.readArray(() => {
			const title = reader.readString(128);
			const description = reader.readString(4096);
			return { title, description };
		});
		return new ClientboundCustomReportDetailsPacket(details);
	}
}