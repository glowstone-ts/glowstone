// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundKeepAlivePacket extends DripleafPacket {
	static readonly id = 0x04;
	static readonly state = State.Configuration;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundKeepAlivePacket.id;
	override readonly state = ClientboundKeepAlivePacket.state;
	override readonly direction = ClientboundKeepAlivePacket.direction;

	constructor(
		public keepAliveId: bigint,
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeLong(this.keepAliveId);
	}

	static read(reader: PacketReader): ClientboundKeepAlivePacket {
		const keepAliveId = reader.readLong();
		return new ClientboundKeepAlivePacket(keepAliveId);
	}
}