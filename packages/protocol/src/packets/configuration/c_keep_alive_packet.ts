// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { Direction, State } from '../../types';
import { GlowstonePacket } from '../../packet';
import { PacketReader, PacketWriter } from '../../buffer';

class ClientboundKeepAlivePacket extends GlowstonePacket {
	override id = 0x04;
	override state = State.Configuration;
	override direction = Direction.Clientbound;

	constructor(
		public keepAliveId: bigint,
	) {
		super();
	}

	serialize() {
		const writer = new PacketWriter();
		writer.writeLong(this.keepAliveId);
		return writer.finish();
	}

	static override deserialize(bytes: Uint8Array): ClientboundKeepAlivePacket {
		const reader = new PacketReader(bytes);
		const keepAliveId = reader.readLong();
		return new ClientboundKeepAlivePacket(keepAliveId);
	}
}

export { ClientboundKeepAlivePacket };