// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ServerboundCustomQueryAnswerPacket extends DripleafPacket {
	static readonly id = 0x02;
	static readonly state = State.Login;
	static readonly direction = Direction.Serverbound;

	override readonly id = ServerboundCustomQueryAnswerPacket.id;
	override readonly state = ServerboundCustomQueryAnswerPacket.state;
	override readonly direction = ServerboundCustomQueryAnswerPacket.direction;

	constructor(
		public messageId: number,
		public response: Uint8Array
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.messageId);
		writer.writeByteArray(this.response);
	}

	static read(reader: PacketReader): ServerboundCustomQueryAnswerPacket {
		const messageId = reader.readVarInt();
		const response = reader.readByteArray();
		return new ServerboundCustomQueryAnswerPacket(messageId, response);
	}
}