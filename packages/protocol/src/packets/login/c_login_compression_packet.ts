// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundLoginCompressionPacket extends DripleafPacket {
	static readonly id = 0x03;
	static readonly state = State.Login;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundLoginCompressionPacket.id;
	override readonly state = ClientboundLoginCompressionPacket.state;
	override readonly direction = ClientboundLoginCompressionPacket.direction;

	constructor(
		public threshold: number
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeVarInt(this.threshold);
	}

	static read(reader: PacketReader): ClientboundLoginCompressionPacket {
		const threshold = reader.readVarInt();
		return new ClientboundLoginCompressionPacket(threshold);
	}
}