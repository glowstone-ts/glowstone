// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ClientboundCookieRequestPacket extends DripleafPacket {
	static readonly id = 0x05;
	static readonly state = State.Login;
	static readonly direction = Direction.Clientbound;

	override readonly id = ClientboundCookieRequestPacket.id;
	override readonly state = ClientboundCookieRequestPacket.state;
	override readonly direction = ClientboundCookieRequestPacket.direction;

	constructor(
		public key: string,
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.key);
	}

	static read(reader: PacketReader): ClientboundCookieRequestPacket {
		const key = reader.readString();
		return new ClientboundCookieRequestPacket(key);
	}
}