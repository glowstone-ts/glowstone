// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket } from '../DripleafPacket';
import { Direction, State } from '../../types';

export class ServerboundHelloPacket extends DripleafPacket {
	static readonly id = 0x00;
	static readonly state = State.Login;
	static readonly direction = Direction.Serverbound;

	override readonly id = ServerboundHelloPacket.id;
	override readonly state = ServerboundHelloPacket.state;
	override readonly direction = ServerboundHelloPacket.direction;

	constructor(
		public name: string,
		public uuid: string
	) {
		super();
	}

	write(writer: PacketWriter) {
		writer.writeString(this.name);
		writer.writeUUID(this.uuid);
	}

	static read(reader: PacketReader): ServerboundHelloPacket {
		const name = reader.readString();
		const uuid = reader.readUUID();
		return new ServerboundHelloPacket(name, uuid);
	}
}