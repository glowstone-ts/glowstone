// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';
import type { UUID } from 'node:crypto';

export class ServerboundHelloPacket extends DripleafPacket {
	static readonly codec = packetCodec<ServerboundHelloPacket>({
		encode(writer: PacketWriter, value: ServerboundHelloPacket) {
		writer.writeString(value.name);
		writer.writeUUID(value.uuid);
		},
		decode(reader: PacketReader): ServerboundHelloPacket {
		const name = reader.readString();
		const uuid = reader.readUUID();
		return new ServerboundHelloPacket(name, uuid);
		},
	});

	constructor(
		public name: string,
		public uuid: UUID
	) {
		super();
	}

}
