// This file is generated by codegen/generate_packets.ts. Do not edit this file manually unless you are Khao who always knows what he is doing.

import { PacketReader, PacketWriter } from '../../buffer';
import { DripleafPacket, packetCodec } from '../DripleafPacket';

export class ClientboundCustomQueryPacket extends DripleafPacket {
	static readonly codec = packetCodec<ClientboundCustomQueryPacket>({
		encode(writer: PacketWriter, value: ClientboundCustomQueryPacket) {
		writer.writeVarInt(value.messageId);
		writer.writeString(value.channel);
		writer.writeByteArray(value.data);
		},
		decode(reader: PacketReader): ClientboundCustomQueryPacket {
		const messageId = reader.readVarInt();
		const channel = reader.readString();
		const data = reader.readByteArray();
		return new ClientboundCustomQueryPacket(messageId, channel, data);
		},
	});

	constructor(
		public messageId: number,
		public channel: string,
		public data: Uint8Array
	) {
		super();
	}

}
